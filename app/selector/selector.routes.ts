import { Route } from '@angular/router';
import { SelectorComponent } from './selector.component';

export const SelectorRoutes: Route[] = [
  {
    path: 'selector',
    component: SelectorComponent
  }
];